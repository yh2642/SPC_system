# coding: utf-8import pickle DATA_FILE = 'static/temp_list/batchlist.txt'def add_batch(batch):	"""	add a batch into batchlist	"""	with open(DATA_FILE, 'rb') as batchlist:		try:			pklist = pickle.load(batchlist)		except EOFError:			pklist = []				pklist.append(item)		with open(DATA_FILE, 'wb') as batchlist:		pickle.dump(pklist, batchlist)def load_list():	"""	output all pricelist in the database	"""	with open(DATA_FILE, 'rb') as batchlist:		try:			pklist = pickle.load(batchlist)		except EOFError: 			pass	return pklistdef modify_item(row, item):	"""	the function modify a certain row of the pricelist, 	the input item is the correct value in that row	"""	with open(DATA_FILE, 'rb') as batchlist:		try:			pklist = pickle.load(batchlist)		except EOFError:			pklist = []				pklist[row] = item		with open(DATA_FILE, 'wb') as batchlist:		pickle.dump(pklist, batchlist)	def remove_item(row):	with open(DATA_FILE, 'rb') as batchlist:		try:			pklist = pickle.load(batchlist)		except EOFError:			pklist = []				pklist.pop(row)		with open(DATA_FILE, 'wb') as batchlist:		pickle.dump(pklist, batchlist)